# This starter workflow is for a CMake project running on multiple platforms. There is a different starter workflow if you just want a single platform.
# See: https://github.com/actions/starter-workflows/blob/main/ci/cmake-single-platform.yml
name: CMake on multiple platforms

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      # Set fail-fast to false to ensure that feedback is delivered for all matrix combinations. Consider changing this to true when your workflow is stable.
      fail-fast: false

      # Set up a matrix to run the following 6 configurations:
      # 1. <Windows, Release, latest MSVC compiler toolchain on the default runner image, default generator>
      # 2. <Linux x64, Release, latest GCC compiler toolchain on the default runner image, default generator>
      # 3. <Linux x64, Release, latest Clang compiler toolchain on the default runner image, default generator>
      # 4. <Linux arm64, Release, latest GCC compiler toolchain on the default runner image, default generator>
      # 5. <Linux arm64, Release, latest Clang compiler toolchain on the default runner image, default generator>
      # 6. <macOS, Release, latest Clang compiler toolchain on the default runner image, default generator>
      #
      # To add more build types (Release, Debug, RelWithDebInfo, etc.) customize the build_type list.
      matrix:
        include:
          - os: ubuntu-26.04
            c_compiler: gcc
            cpp_compiler: g++
            build_type: Release
          - os: ubuntu-26.04
            c_compiler: clang
            cpp_compiler: clang++
            build_type: Release
          - os: ubuntu-26.04-arm
            c_compiler: gcc
            cpp_compiler: g++
            build_type: Release
          - os: ubuntu-26.04-arm
            c_compiler: clang
            cpp_compiler: clang++
            build_type: Release
          - os: windows-latest
            c_compiler: clang
            cpp_compiler: clang++
            build_type: Release
          - os: macos-latest
            c_compiler: clang
            cpp_compiler: clang++
            build_type: Release

    steps:
    - uses: actions/checkout@v7
      with:
        fetch-depth: 0

    - name: Install LLVM on Windows
      if: matrix.os == 'windows-latest'
      run: choco install llvm -y

    - name: Set reusable strings
      # Turn repeated input strings (such as the build output directory) into step outputs. These step outputs can be used throughout the workflow file.
      id: strings
      shell: bash
      run: |
        echo "build-output-dir=${{ github.workspace }}/build" >> "$GITHUB_OUTPUT"

    - name: Install dependencies on macOS
      if: matrix.os == 'macos-latest'
      run: brew install json-c pcre2 pkg-config ninja

    - name: Build dependencies on Windows
      if: matrix.os == 'windows-latest'
      working-directory: windows
      run: .\build_deps.bat

    - name: Install dependencies on Linux
      if: matrix.os == 'ubuntu-26.04' || matrix.os == 'ubuntu-26.04-arm'
      run: |
        sudo apt-get update
        sudo apt-get install -y libjson-c-dev libpcre2-dev

    - name: Generate Language Definiiton headers
      working-directory: ${{ github.workspace }}/definitions
      run: > 
        ./regenerate.sh

    - name: Configure CMake
      if: matrix.os != 'windows-latest'
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      run: >
        cmake -B ${{ steps.strings.outputs.build-output-dir }}
        -DCMAKE_CXX_COMPILER=${{ matrix.cpp_compiler }}
        -DCMAKE_C_COMPILER=${{ matrix.c_compiler }}
        -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}
        -DBUILD_TESTING=${{ matrix.os != 'windows-latest' && 'ON' || 'OFF' }}
        -S ${{ github.workspace }}
        -G "Ninja"

    - name: Build
      if: matrix.os != 'windows-latest'
      # Build your program with the given configuration. Note that --config is needed because the default Windows generator is a multi-config generator (Visual Studio generator).
      run: cmake --build ${{ steps.strings.outputs.build-output-dir }} --config ${{ matrix.build_type }}

    - name: Build
      if: matrix.os == 'windows-latest'
      working-directory: windows
      run: .\build.bat

    - name: Test
      if: matrix.os != 'windows-latest'
      working-directory: ${{ steps.strings.outputs.build-output-dir }}
      # Execute tests defined by the CMake configuration. Note that --build-config is needed because the default Windows generator is a multi-config generator (Visual Studio generator).
      # See https://cmake.org/cmake/help/latest/manual/ctest.1.html for more detail
      run: ctest --build-config ${{ matrix.build_type }} --output-on-failure
